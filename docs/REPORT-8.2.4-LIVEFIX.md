# Отчёт 8.2.4 — Live-находки (сразу после 8.2.3)

**Версия 8.2.4 · b818 · тесты 310 (+2) · ruff 0 · check.sh PASS · gui_check PASS · задеплоено**

## Что нашли живьём (и исправили)

1. **Повторные DELETE уже снятого тест-поста.** После TTL-автоочистки пост получал `test_auto_cancelled`,
   но оставался в «активных» → каждый цикл (и каждый рестарт) снова пытался его удалить; Postiz на stale id
   отвечает **500**, в журнале — трассы ретраев. Фикс: `test_auto_cancelled` исключается из активных.
   **Live-проверка:** после деплоя в журнале нового процесса — **0** строк `DELETE … cmub6ne6j`.
2. **SECURITY: утечка Telegram bot-token в journal.** `httpx` на уровне INFO печатает полный URL запроса,
   включая `https://api.telegram.org/bot<TOKEN>/getUpdates`. Фикс: `httpx`/`httpcore` → WARNING при старте.
   **Live-проверка:** в журнале нового процесса (PID 684393) — **0** вхождений `api.telegram.org/bot<digits>`.

## Прочее из журнала (не дефекты)
- `Traceback`/`[ERROR]` в старом процессе — это `logger.warning(..., exc_info=True)` авто-очистки
  (уровень WARNING, не сбой цикла); после фикса (1) исчезают вместе с повторами.

## Gate
`pytest 310` · `ruff 0` · `check.sh` PASS · `gui_check` PASS (боевой b818) · панель 200 ·
боевые счётчики 39/39/4 без изменений.

## Residual (без изменений)
- `style-src 'unsafe-inline'` (style-атрибуты UI) — план: рефакторинг в классы.
- 429 Bot API в проде не форсировался (юнит зелёный).
