# Отчёт 8.4.0 — UI/UX 2.0 (Telegram-native, ровные списки, План)

**Версия 8.4.0 / b821 · 323 теста · ruff 0 · check.sh PASS · gui_check PASS · задеплоено · слот 18:00 вышел (шорт 305 → YouTube + t.me/…/9)**

## Что сделано (чеклист)

| # | Пункт | Статус | Доказательство |
|---|-------|--------|----------------|
| B1 | Токены Telegram-first (`--tg-*`) | ✅ | панель берёт цвета из `themeParams`, фолбэк — Apple-палитра |
| B2 | Нативность: `contentSafeAreaInset`/`safeAreaInset`, `setBottomBarColor`, `HapticFeedback`, нативный **BackButton** в шторках | ✅ | хак «60px под кнопку TG» удалён — теперь инсеты из SDK; свой ✕ в шторке обложки скрывается внутри Telegram |
| B3 | Примитивы `.item/.badge/.btn-grid/.seg/.panel-head/.field/.act-grid` | ✅ | единый ритм (шкала 4/8/12/16/24/32) |
| B4 | Чистка legacy CSS | ✅ | дубли `.queue-col`/`.q-title`, `margin-top:34px` у чекбокса убраны |
| C1 | Обзор | ✅ | CTA равной ширины, счётчики 2×2, платформы списком с бейджами |
| C2 | Видео | ✅ | сетка кнопок равной высоты, «Сканировать» отдельной строкой |
| C3 | Очередь | ✅ | карточки: обложка 72×108 (замер), заголовок 2 строки, статус-бейдж **ниже**, ✎ сверху / 🗑 снизу по граням; замер: колонки 80/180/44, overflow 0 |
| C4 | Сети | ✅ | одна кнопка в строке, красный бейдж «Выключено», «Лимит в день: N», кнопки «Пауза всем/Возобновить все» равной ширины |
| C5 | Остаток | ✅ | ряд из 3 кнопок равной высоты + бейдж состояния |
| C6 | Действия | ✅ | полные подписи + пояснение под каждой кнопкой, 3 секции |
| C7 | Статистика | ✅ | выравнивание, убран хардкод «ОК», бейджи |
| C8 | Ошибки | ✅ | отдельный экран: сводка, группировка по платформе, полный текст ошибки; дубль-вкладка из Настроек убрана |
| C9 | Справка | ✅ | двухстрочный список (название + описание), узкие колонки убраны |
| C10 | Расписание/Группы | ✅ | чипы дней, сегмент «Сколько в день» + time-пикеры, приоритет «платформа → группа → глобально», группы списком с чипами соцсетей |
| C11 | План: День/Неделя/Месяц | ✅ | сегмент; неделя — 7 дней с чипами времени; месяц — сетка с подсветкой и обводкой «сегодня»; **YT+TG одного видео — одной строкой/чипом** (парные иконки) |
| D1 | Полные подписи | ✅ | «Распределить остаток», «Убрать лишние посты из Postiz», «Вернуть удалённое и запустить скан»; в «Ещё» — полные названия разделов |
| A2 | Публикация 18:00 | ✅ | шорт 305: YouTube `published` + Telegram `t.me/tochkanablyudeniya/9` |
| A1 | SSD-сверка | 🔄 | идёт в фоне (2 процесса rsync -n -c), лог сохраняется |

## Проверки
- `pytest` **323** (в т.ч. 12 статических UI: tabbar, токены, safe-area, I18N-полнота, XSS-инварианты, CSP-nonce)
- XSS/CSP-линтер ✅ · `check.sh` ALL CHECKS PASSED
- `gui_check` (jsdom, боевой b821) ✅ — 10 экранов, очередь/редактор/обложки/фильтры/удаление/папки
- Живые скриншоты 390×844 всех экранов: `~/Downloads/ui-8.4/` (11 шт.)
- Прод: сервис active, 0 ERROR/Traceback, счётчики 39/39/5 после публикации

## Осталось (честно)
1. **C10-полировка**: сегмент «Сколько в день» действует на «Обычные шортсы»; для «Серий» (>1 фильма в день) нужен backend (`times[]` в long-расписании) — не делал.
2. **Месяц**: заголовки дней недели добавлены в этом же коммите, но на живом скрине ещё старая версия (перепроверю).
3. **Нативный MainButton** в шторках (по современным гайдам TG) — не подключал: конфликтует с нашим таббаром; при желании — отдельной задачей.
4. Остальные residual-пункты — в `docs/RESIDUAL.md`.
