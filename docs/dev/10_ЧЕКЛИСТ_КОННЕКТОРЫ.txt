================================================================================
  РЕВИЗИЯ: «АБСОЛЮТНО ВСЕ КОННЕКТОРЫ И МОДУЛИ» — ЧЕСТНЫЙ СТАТУС
  Дата: 25.09.2026. Проверено вживую (master 444d8e7, прод, Postiz, VM, доки).
  Вопрос владельца: достаточно ли YouTube-CLI (docs/dev/09) для «всех коннекторов»?
  Ответ: НЕТ. CLI — «руки» только для YouTube. Ниже полная карта из трёх слоёв.
================================================================================

1. ФАКТЫ НА 25.09.2026 (живые проверки, не из памяти)
--------------------------------------------------------------------------------
- Прод-оркестратор: 3823 цикла, 0 ошибок, очередь 62; engines youtube: direct,
  telegram: postiz; деплой активен.
- Postiz — подключено ровно 2 канала: YouTube @testPostiz, Telegram «Точка
  наблюдения». Facebook / Instagram / TikTok / Threads — НЕ подключены.
- Postiz env: FACEBOOK_APP_ID/SECRET, THREADS_APP_ID/SECRET, TIKTOK_CLIENT_ID/
  SECRET — не заданы (PLATFORM_SETUP.md + проверка контейнера 23.09).
- Наружу Postiz открыт через quick tunnel trycloudflare (адрес меняется при
  рестарте → ломает Redirect URI всех OAuth).
- Meta: FB-страница «Точка наблюдения» создана (ID 61594683901231); регистрация
  разработчика стоит на шаге Contact info (mail.ru отклонён); профили
  Instagram и Threads у владельца уже есть.
- TikTok: вход в developers.tiktok.com по телефону невозможен — нужен email
  аккаунта (пароль есть).
- CI GitHub Actions: success; ./scripts/check.sh — ALL CHECKS PASSED (565).

2. СЛОЙ КОДА (делает Grok; внешними доступами не заблокирован)
--------------------------------------------------------------------------------
[ГОТОВО] Э1 — каркас модульной системы.
[ГОТОВО] P1 — фиксы (force_update, watchdog-алерты, регресс 24.09).
[ЖДЁТ]   P2 — модуль YouTube (эталон), по docs/dev/07_GROK_TASK.txt.
[ЖДЁТ]   P3 — модуль Telegram (перенос Bot API-логики).
[ЖДЁТ]   P4 — адаптер Postiz.
[ЖДЁТ]   P5 — переключение прода (после P2–P4).
[ЖДЁТ]   P6 — модули IG / TikTok(inbox) / FB / Threads + media host (B2).
[ЖДЁТ]   P7 — сайт для аудита TikTok + комплект подачи.
[ЖДЁТ]   09 — YouTube-CLI «руки ассистента» (docs/dev/09_YT_SCRIPTS_TZ.txt;
         дополняет P2, не заменяет).
Важно: даже идеально написанный код не опубликует ни одного поста, пока не
закрыт слой коннекторов (ниже).

3. СЛОЙ КОННЕКТОРОВ (внешние доступы; главный тормоз)
--------------------------------------------------------------------------------
YouTube:
  Сейчас: тестовый канал работает через Postiz; движок direct (токен из брокера).
  Нужно: 1) боевой канал (владелец); 2) подтверждение канала по телефону —
     без него НЕ работают кастомные обложки (факт 23.09); 3) OAuth-приложение
     Google Cloud в «In production» — иначе refresh-токены живут 7 дней; свои
     client_id/secret для прямого модуля (сейчас в окружении Postiz — перенести
     по 07 §2.4); 4) при надобности — заявка на квоту (10 000 units/сутки ≈
     6 загрузок видео).
  Кто: владелец (канал, верификация, GCP).
Telegram:
  Сейчас: работает (через Postiz).
  Нужно: ничего внешнего; для прямого модуля — бот-токен (есть) + бот-админ
     канала (проверить фактом).
  Кто: никто — самая разблокированная площадка.
Facebook:
  Сейчас: страница создана; приложение Meta НЕ создано; ключей в Postiz нет.
  Нужно: Meta-регистрация (стоит на email) → приложение Meta → FACEBOOK_APP_ID/
     SECRET в Postiz → Redirect URI (стабильный адрес!).
  Примечание: App Review и Business Verification НЕ нужны, пока подключаются
     свои аккаунты (владелец — админ приложения, Development Mode).
  Кто: владелец (email) + агент (настройка ключей/туннеля).
Instagram:
  Сейчас: профиль есть; приложения/ключей нет.
  Нужно: то же Meta-приложение; IG-профиль привязать к странице; ПУБЛИЧНЫЙ
     медиа-URL (B2) — Meta сама скачивает ролик по ссылке.
  Кто: владелец + B2 (аккаунт/ключи).
Threads:
  Сейчас: профиль есть; ключей нет.
  Нужно: ОТДЕЛЬНОЕ приложение Threads API (THREADS_APP_ID/SECRET) — Meta
     требует отдельное; публичный медиа-URL (B2).
  Кто: владелец + B2.
TikTok:
  Сейчас: аккаунт/пароль есть; вход разработчика невозможен без email.
  Нужно: email аккаунта → developer-аккаунт → app (Login Kit + Content Posting
     API) → клиенты в Postiz → callback (стабильный адрес). Прямая публикация —
     только после аудита; до него draft/SELF_ONLY (fallback inbox уже в P6).
  Кто: владелец (email) + агент (подача аудита).
Postiz (путь «через Postiz»):
  Сейчас: 2 канала; provider-ключи не заданы.
  Нужно: либо свои приложения по каждой сети + env в docker-compose, либо
     Postiz Cloud $29 (их verified apps + их медиахостинг — закрывает разом
     Meta/TikTok/медиа).
  Кто: решение владельца.

4. ОБЩИЕ ВНЕШНИЕ ЗАВИСИМОСТИ (легко забыть)
--------------------------------------------------------------------------------
- Стабильный адрес/домен: quick tunnel trycloudflare меняется при рестарте →
  ломает OAuth callback всех площадок. Нужен постоянный домен или named tunnel.
- Email + сайт (privacy/terms/support): нужен TikTok-аудиту (сайт сделает P7 на
  GitHub Pages) и для шага Contact info в Meta. Нужны адреса от владельца.
- B2 (решение принято 24.09, аккаунта нет): бакет + ключи; без него IG/Threads
  не опубликуются; также используется как media_host в P6.1.
- Тестовые аккаунты для contract-проверок: YouTube testPostiz ✓, Telegram ✓,
  IG/FB/Threads/TikTok — профили владельца (согласовать использование).
- Решение: Postiz Cloud $29/мес (быстро, их приложения и хостинг) vs свои
  приложения (0 ₽, но дольше и больше поддержки).

5. ЧЕК-ЛИСТ ВЛАДЕЛЬЦУ (по приоритету: что разблокирует больше всего)
--------------------------------------------------------------------------------
[ ] 1. Email для Meta (шаг Contact info) — ящик, который Facebook примет.
[ ] 2. Email для TikTok-аккаунта (доступ к почте) — вход разработчика.
[ ] 3. YouTube: боевой канал; подтверждение канала (телефон); Google Cloud
       OAuth-приложение → «In production».
[ ] 4. B2: создать аккаунт/бакет, прислать keyID/key (0 ₽, без карты).
[ ] 5. Домен (или named cloudflare tunnel) для стабильных callback.
[ ] 6. Решение по Postiz Cloud ($29/мес): да/нет.
[ ] 7. Согласовать тестовые аккаунты IG/FB/Threads/TikTok для contract-тестов.

6. ЧТО НЕ ЗАБЛОКИРОВАНО (можно делать уже сейчас)
--------------------------------------------------------------------------------
- Grok: P2–P7 + 09 (код, тесты, CI) — без внешних доступов.
- Агент: шаблоны сайта аудита (P7), тексты подачи Meta/TikTok, проверка Postiz
  env при получении ключей, отчёт tunnel sync.

7. СВЯЗАННЫЕ ДОКУМЕНТЫ
--------------------------------------------------------------------------------
- docs/dev/07_GROK_TASK.txt §9 («что нужно от владельца»)
- docs/dev/09_YT_SCRIPTS_TZ.txt (YouTube-CLI)
- docs/PLATFORM_SETUP.md, docs/POSTIZ_SOCIAL_SETUP_STATUS.md, docs/STORAGE-OPTIONS.md
- docs/dev/04_DECISIONS.txt (A17 TikTok callback, A19 FILE_UPLOAD, Q2 B2)
================================================================================
